import type { MetadataRoute } from "next";
import { navigation } from "../lib/site-data";
import { publications, researchUpdates } from "../lib/site-content-v2";
import { getSiteUrl } from "../lib/site-config";
export default function sitemap(): MetadataRoute.Sitemap { const base = getSiteUrl(); const notes = researchUpdates.filter((entry) => entry.contentType === "researchNote").map((entry) => ({ url: `${base}/research-notes/${entry.slug}`, lastModified: new Date("2026-09-18"), changeFrequency: "yearly" as const, priority: .7 })); const publicationPages = publications.map((publication) => ({ url: `${base}/publications/${publication.slug}`, lastModified: new Date("2026-09-18"), changeFrequency: "yearly" as const, priority: .7 })); return [{ url: base, changeFrequency: "monthly", priority: 1 }, ...navigation.map(([, href]) => ({ url: `${base}${href}`, changeFrequency: "monthly" as const, priority: href === "/research" || href === "/publications" ? .8 : .6 })), ...publicationPages, ...notes]; }
